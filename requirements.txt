# DrowsyGuard — Python dependencies
# Install: pip install -r requirements.txt

# Core CV & ML
opencv-python>=4.8.0
dlib>=19.24.0
imutils>=0.5.4
scipy>=1.11.0
numpy>=1.24.0

# Deep learning (CNN classifier — optional but recommended)
tensorflow>=2.13.0      # or tensorflow-cpu for CPU-only machines

# Audio alert
playsound==1.2.2        # 1.3.0 has known Linux bugs — pin to 1.2.2

# Dev / testing
pytest>=7.4.0
